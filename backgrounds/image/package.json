{
  "name": "backgroundImageProcessor",
  "version": "1.0.0",
  "description": "No Description",
  "scripts": {
    "application": "node dist/index.js",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log,./caches,./calculator_caches --copy-files",
    "start": "npm run build && node dist/index.js"
  },
  "license": "MIT",
  "dependencies": {
    "async-request": "^1.2.0",
    "await-sleep": "0.0.1",
    "base64-img": "^1.0.4",
    "canvas": "^1.6.5",
    "dotenv": "^6.1.0",
    "express": "^4.16.4",
    "fs.promised": "^3.0.0",
    "image-watermark-2": "0.0.9",
    "merge-images": "^1.1.0",
    "mysql2": "^1.6.2",
    "request": "^2.88.0",
    "sharp": "^0.21.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^2.6.2"
  }
}
